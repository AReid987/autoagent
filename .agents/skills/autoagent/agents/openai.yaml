interface:
  display_name: "Autoagent"
  short_description: "Repo-specific patterns and workflows for autoagent"
  default_prompt: "Use the autoagent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true